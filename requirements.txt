# Core -- needed by models/, training/, tests/
torch>=2.0
numpy>=1.24
sympy>=1.12

# Experiments and benchmarks
scikit-learn>=1.3
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
torchvision>=0.15

# experiments/baseline_comparison.py
xgboost>=2.0
seaborn>=0.12

# OPTIONAL -- none of the below is needed to run the experiments or the tests.
# app.py + training/visualization/dashboard.py (Streamlit dashboard)
streamlit>=1.30
plotly>=5.18

# benchmarks/generate_report.py
fpdf2>=2.7

# Was needed by legacy/nas_comparison_mnist.py, which no longer runs (see
# legacy/README.md). Heavy and version-sensitive; install separately if you
# ever restore the DARTS/ENAS comparison.
#   nni>=3.0
#   pytorch-lightning>=2.0
